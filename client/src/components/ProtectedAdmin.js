// import React from 'react';
// import { Route, Redirect } from 'react-router-dom';

// const ProtectedAdmin = ({ component: Component, authenticated, ...rest }) => {
//   return <Route render={(props) => (authenticated ? <Component {...props} /> : <Redirect to="/adminlogin" />)} {...rest} />;
// };

// export default ProtectedAdmin;